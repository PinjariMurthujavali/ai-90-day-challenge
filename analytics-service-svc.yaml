# ============================================
# DAY 50: Kubernetes Clusters Setup
# ============================================
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: ai-chatbot
spec:
  selector:
    app: analytics-service
  ports:
    - port: 5001
      targetPort: 5001
  type: ClusterIP
